#  Image Metadata Encoder (Python)

A simple but structured Python script for adding **custom metadata** to images using **Pillow**.

Currently supports:
- PNG text metadata
- Clean, extendable code structure
- Automatic timestamps

---

## Requirements

- Python 3.8+
- Pillow

Install dependencies:

```bash
pip install pillow

